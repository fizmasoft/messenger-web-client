/**
 * Returns a string.
 * @param fractionDigits Number of digits after the decimal point. Must be in the range 0 - 20, inclusive.
 */
export declare function numberFormat(number: number, fractionDigits?: number, dsep?: string, tsep?: string): string;
export declare function numberWithZero(num: number, zeroLen: number): string;
